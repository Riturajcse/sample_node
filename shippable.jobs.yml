jobs:
  - name: gke-man
    type: manifest
    steps:
      - GET: gke-sn
      - GET: trigger-gke-man

  - name: gke-upgrade-deploy
    type: gkeDeploy
    options:
      deployMethod: upgrade
    steps:
      - GET: gke-man
      - GET: gke-cluster
