jobs:
  - name: gke-man
    type: manifest
    steps:
      - GET: gke-nginx
      - GET: image-opts
      - GET: trigger-gke-man

  - name: gke-deploy-2
    type: gkeDeploy
    options:
      deployMethod: blueGreen
    steps:
      - GET: gke-man
      - GET: gke-cluster