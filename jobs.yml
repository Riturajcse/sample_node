jobs:
  - name: tri-manifest
    type: manifest
    steps:
      - GET: tri-nginx
      - GET: tri-params

  - name: tri-deploy
    type: tripubDeploy
    steps:
      - GET: tri-manifest
      - GET: tri-cluster
    
